# Environment Variables for ConverseX

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/conversex

# JWT Secret (Change this to a random, secure string in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Client URLs (for CORS) - support comma separated list, e.g. prod + preview + local
CLIENT_URLS=http://localhost:5173,https://your-app.vercel.app
